using UnityEngine;

public class GithubTest : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        // 안녕하세요. 테스트입니다.
        // 지금은 깃허브 연동 테스트 중 입니다.
        // 연동애 잘 되기를
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
